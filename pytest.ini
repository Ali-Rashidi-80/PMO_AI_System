[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
testpaths = tests
markers =
    live: requires LM Studio running
    golden: live E2E golden path
    integration: needs optional stack
pythonpath = services/lmstudio-gateway
